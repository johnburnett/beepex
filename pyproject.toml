[project]
name = "beepex"
version = "0.1.0"
description = "Export chat history from the Beeper desktop client"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.5",
    "rich>=14.1.0",
]
